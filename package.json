{
  "name": "BkmrksBot",
  "version": "1.0.0",
  "description": "A simple bot that can add bookmarks to Pocket via telegram",
  "main": "index.js",
  "scripts": {
    "start": "babel-node index --presets=env"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.0.0",
    "telegraf": "^3.22.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
